def test1():
	print('hello test')


class class1:
	def __init__(_open, _high, _low, _close, _volume, _unVol, _change, _changePer, _vwap, _label, _changeOverTime):
		open = _open
		high = _high
		low = _low
		close = _close
		volume = _volume
		unadjustedVolume = _unVol
		change = _change
		changePercent = _changePer
		vwap = _vwap
		label = _label
		changeOverTime = _changeOverTime
